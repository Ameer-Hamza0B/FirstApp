<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teachers List</title>
  <style>
    /* General Styles */
    body {
      font-family: 'Arial', sans-serif;
      background-image: url('https://wallpapers.com/images/featured/beautiful-blue-background-iz2zgnaheo3etro2.jpg'); /* Matches the home page background */
      background-size: cover;
      background-position: center;
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* Header Styles */
    header {
      background-color: rgba(0, 0, 0, 0.8);
      padding: 20px;
      text-align: center;
      position: relative;
      z-index: 1;
    }

    header h1 {
      margin: 0;
      font-size: 2.5em;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
    }

    nav {
      margin: 20px 0;
    }

    nav a {
      color: #fff;
      text-decoration: none;
      margin: 0 15px;
      font-size: 1.2em;
      transition: color 0.3s;
    }

    nav a:hover {
      color: #007bff;
    }

    /* Main Content Styles */
    main {
      flex: 1;
      padding: 20px;
      overflow-y: auto;
    }

    h1 {
      text-align: center;
      color: #fff;
      text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.7);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
      background-color: rgba(0, 0, 0, 0.6); /* Semi-transparent background */
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
    }

    thead {
      background-color: #007bff;
      color: white;
    }

    th, td {
      padding: 12px;
      text-align: left;
      border: 1px solid #dee2e6;
    }

    tbody tr:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }

    tbody tr:nth-child(even) {
      background-color: rgba(255, 255, 255, 0.05);
    }

    tbody tr:nth-child(odd) {
      background-color: rgba(0, 0, 0, 0.05);
    }

    /* Styling for the "No teachers found" message */
    .no-teachers {
      text-align: center;
      font-style: italic;
      color: #ccc;
    }

    /* Footer Styles */
    footer {
      background-color: rgba(0, 0, 0, 0.8);
      text-align: center;
      padding: 10px;
      position: relative;
      z-index: 1;
    }

    footer p {
      margin: 0;
      color: #fff;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      h1 {
        font-size: 2em;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <h1>Teachers List</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/users">Users</a>
      <a href="/teachers">Teachers</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
    </nav>
  </header>

  <!-- Main Content -->
  <main>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Subject</th>
          <th>Age</th>
          <th>Students</th> <!-- New column for Students -->
        </tr>
      </thead>
      <tbody>
        <% if @teachers.present? %>
          <% @teachers.each do |teacher| %>
            <tr>
              <td><%= teacher.id %></td>
              <td><%= link_to teacher.name, teacher_path(teacher) %></td>
              <td><%= teacher.subject %></td>
              <td><%= teacher.age %></td>
              <td>
                <% if teacher.students.any? %>
                  <ul>
                    <% teacher.students.each do |student| %>
                      <li><%= student.username %></li> <!-- Displaying each student's username -->
                    <% end %>
                  </ul>
                <% else %>
                  <span>No students assigned</span>
                <% end %>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="5" class="no-teachers">No teachers found.</td> <!-- Adjusted colspan to match the number of columns -->
          </tr>
        <% end %>
      </tbody>
    </table>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; 2024 The World. All rights reserved.</p>
  </footer>
</body>
</html>
